{
  "name": "job-portal-chatbot",
  "version": "1.0.0",
  "description": "Production-ready recruitment chatbot for JOB PORTAL GLOBAL (Shopify + OpenAI + n8n + Google Sheets + Telegram). Handles 1,000+ candidates/day with spam filtering, duplicate detection, and async queue-based submission.",
  "main": "src/server.js",
  "type": "commonjs",
  "engines": {
    "node": ">=18.0.0"
  },
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch src/server.js",
    "test": "node --test \"tests/*.test.js\"",
    "lint": "node --check src/server.js && node --check src/app.js",
    "check": "node scripts/check-env.js",
    "extract": "node scripts/extract-pdf.js",
    "pdf": "node scripts/generate-pdf.js",
    "build:knowledge": "npm run extract && npm run pdf"
  },
  "dependencies": {
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "helmet": "^7.1.0"
  },
  "license": "MIT",
  "devDependencies": {
    "pdf-parse": "^1.1.1",
    "pdfkit": "^0.19.1"
  }
}
