# Taste Profile
- Communicates in Hinglish (English mixed with Roman Urdu/Hindi) with an informal, energetic tone; specs can mix languages and use emphatic phrasing like "GOOOO!" and "Ye website hai". Confidence: 0.7
- Wants the agent to make decisive choices for them instead of presenting options — e.g., "just pick one cheap vps" — and prefers budget-friendly/cost-effective infrastructure and tooling; hosted the backend on a free Cloudflare tunnel and proposed serving the widget from their already-owned Shopify domain ("we have a shopify domain, we can host the chatbot there instead") rather than paying for new hosting. Confidence: 0.8
- Writes dense, all-in-one task briefs (objective, tech stack, flow, requirements, deliverables) rather than incremental back-and-forth. Confidence: 0.7
- Wants production-ready, at-scale systems: explicitly targets 1,000+ candidates/day and expects async/queue-friendly handling, fast API responses, retry with backoff, timeout protection, logging, duplicate detection, and spam prevention. Confidence: 0.9
- Requires secrets to come only from environment variables with a documented `.env.example` template — no hardcoded secrets — plus boot-time environment validation. Confidence: 0.9
- Prefers modular architecture, clean folder structure, well-documented, easily maintainable code with proper error handling and secure input validation. Confidence: 0.9
- Expects complete deliverables beyond code: README documentation, installation instructions, VPS deployment instructions, and ready-to-import integration files (e.g., workflow JSONs) — explicitly requested a clean replacement n8n workflow JSON to import directly when the live one was broken. Confidence: 0.95
- Prefers self-hosted infrastructure on their own VPS (e.g., self-hosting n8n with Docker) over managed cloud/SaaS options — explicitly confirmed they bought a Contabo VPS for this purpose, questioned whether an n8n.com cloud account was even needed, and then moved to deploy n8n onto that VPS. Confidence: 0.9
- Prefers domain-free VPS setups over buying a domain: internal services bound to localhost with admin access via SSH tunnel (chose the no-domain option when asked whether they had a domain for n8n). Reconfirmed when a stable public URL was needed — "no domains" — and asked for a workaround instead of buying one. Confidence: 0.8
- Prefers automated, self-healing workarounds over paying for stable infrastructure: proposed auto-updating the changing tunnel URL on every VPS reboot ("once the VPS reboost, we auto-update the URL?") rather than buying a domain or setting up a Cloudflare account; embraces free-tier limitations (dynamic trycloudflare URLs, GitHub raw hosting) and engineering around them. Confidence: 0.6
- Wants deployed automations verified end-to-end before sign-off — e.g., a smoke test proving a lead flows through the whole pipeline (webhook → Google Sheets row → owner's Telegram notification), not just "deployed and running". Confidence: 0.7
- Splits deployment work by machine: the agent handles everything on the dev/repo side (staging, committing, pushing — "push the changes for me") while the user personally runs only the VPS-side commands ("i will only run the vps command") — so when handing off a deploy, do the local git work yourself and hand the user only the server-side command sequence. Reconfirmed in a later session: "push the changes to git and then all i need to run that vps command to deploy right?" — the agent pushed locally and handed only the ordered VPS commands. Reconfirmed again with a bare "push the changes." — the agent staged the relevant files, committed with a detailed message, pushed, and handed only the VPS command sequence (git pull, npm ci, restart, health check) plus the extra n8n re-import step. Reconfirmed again with a terse "commit and push so i can deploy ot to vps" — the agent committed the code changes with a detailed message, pushed to origin/main, and handed only the server-side pull + restart commands. Confidence: 0.97
- Wants step-by-step guided instructions for server/ops setup that they can follow and run themselves ("guide me through the setup and I will follow"), rather than the agent performing the steps for them or fully automating everything. Reconfirmed when asking where/how to import the n8n workflow (wants the concrete "open editor → Import from File → attach credentials → Activate" walkthrough, including exact UI clicks). Confidence: 1.0
- Deploy hand-offs are change-scoped for this project: chatbot-code-only changes need just the VPS sequence (git pull, npm ci --omit=dev, systemctl restart, health check), while n8n workflow changes additionally require re-importing the workflow JSON — the agent should state which applies in the hand-off so the user knows whether the extra step is needed. Reconfirmed when the agent's hand-off explicitly called out "Chatbot-code only again, so no n8n re-import needed" after a code-only commit. Confidence: 0.7
- Does not retain the deployment/ops procedure between sessions — asked "how do i deploy to my vps again?" and expected the agent to re-supply the exact ordered command sequence (commit + push → on VPS: git pull, npm ci --omit=dev, systemctl restart, health check, and any tunnel-URL updates) rather than being pointed at the repo's deploy script/README to figure it out. Reconfirmed right after a deploy hand-off when the user again asked "to deploy, what should I do, what are my steps" — the agent re-supplied the full two-part sequence (dev-side commit of the then-untracked knowledge base, then VPS git pull → npm ci --omit=dev → systemctl restart → health check) in full rather than referencing docs. Confidence: 0.7
- Wants AI assistants strictly grounded in an approved knowledge base (the "PDF" of the website): the bot answers only from that content and redirects users to the website for anything out of scope, except greetings/small talk. Reconfirmed when the bot was already working: asked to add explicit handling for "sentimentals" — small-talk messages like "how are you" and similar get their own friendly responses, while every other out-of-context question (anything that isn't sentimental) gets redirected — i.e., small talk is the ONLY category the bot should answer outside the knowledge base, everything else redirects. Reconfirmed when the user asked to verify the migration state ("any old knowledge-base is all gone right? ... we are only using the provided pdf by client ... anything else is declared irrelevant yeah?"): the client PDF is the sole knowledge source, the old store-scrape knowledge base must be fully gone, and the user expects the agent to actually verify this claim (grep the repo) rather than just assert it — flagging leftover dead artifacts (orphaned scrape files, stale README/package.json/.env.example references) that contradict the PDF-only story. Confidence: 0.97
- When a candidate asks about a job or asks for job instructions ("how to apply for X", "X kaise karna hai", "data entry apply kaise karein"), the bot must give proper, full job details from the knowledge base — summary plus Requirements / Why join / How to apply when that data exists — not a one-line summary, and never a redirect. Confidence: 0.7
- Prefers hard-coded rules (validation, guardrails, redirects) to be deterministic logic rather than left to the model's judgment. Confidence: 0.8
- Wants deterministic handlers ordered by intent priority so ambiguous phrases resolve to the correct action: a back-out/cancel ("no thanks", "nahi") during field collection must beat the sentiment handler (which would otherwise reply "you're welcome" and keep asking forever), while dismissive phrases ("no problem", "no worries") must NOT close the application — overlapping detectors need explicit precedence rules and the per-state checks consolidated so they don't re-fire with different behavior. Confidence: 0.7
- When migrating/renaming components (e.g., Gemini → OpenAI), prefers a full, consistent rename across code, config, env vars, README, tests, and deploy scripts over a minimal in-place swap that leaves misleading names behind — chose the "full rename" option even though it touched more files. Confidence: 0.7
- Wants minimal, single-action user hand-offs in the bot flow: after guidance (e.g., VPN steps), present exactly one link/action (the Telegram join link) and nothing else — no extra tutorial links or follow-up prompts ("that's it"). Reconfirmed explicitly again in later messages: "the chatbot will just give this link to the user, instead of any telegram tokenbot or anything. just the link." Confidence: 1.0
- Later evolved the hand-off into a full guided apply flow, specified turn-by-turn with a worked example: SHORT greeting (small intro to the site, no pitch, no links) → job Q&A from the knowledge base → the Telegram pitch + setup links (Proton VPN / app / YouTube tutorial) only AFTER the candidate shows interest → "apply? yes/no" → collect Name → Contact Number → Telegram username/number → confirm → invite link given only at the very end, together with the sheet submission. When the agent's earlier version dumped the full pitch in the greeting, the user rejected it ("the flow, is, not correct") and re-specified this ordering — pitch deferred until interest, invite link last. Confidence: 0.9
- Wants the guided flow to tolerate many input variations and use-cases rather than a rigid scripted path — "should accept tons of use-cases", "Create work-arounds, and all" — the bot should handle whatever phrasing or intent path a candidate takes without getting stuck. No dead-end states: after a completed submission, any new message ("hey", "hi", a job question) must reset to a fresh conversation (short greeting) instead of repeating "your application has been received" — the user reported exactly this stuck state as a bug — while submission-level duplicate detection still protects the sheet from double entries. Reconfirmed when demanding the flow handle loose job-name phrasings ("graphic design" → "Graphic Designer") without redirecting to the out-of-scope guardrail, and job-selection questions asked mid-collection ("konsi job ke liye apply kar rahe?") answered instead of stored as the field value. Extended with the mid-process side-question rule: a relevant side question asked mid-flow — security/privacy concerns ("mujhe security concerns hain" while being asked for name/phone), job-details questions, or sentimentals ("thanks", "no problem") — must be answered AND the field re-asked so the application flow is never lost ("mid-level process me kuch aur puch sakta hai if relevant hai tou chatbot ko reply karna chahiye"); the side answer must never be swallowed, stored as the field value, or kill the pending flow. Extended again: the guard applies to ANY mid-collection question — relevant or irrelevant ("user can mid-ask ASK ANY RELEVANT, ir-relevant question, chatbot must handle those scenarios instead of taking any other question or statement as the user name, phone, telegram") — and across ALL user-information fields (name, phone, telegram); interest expressions may also arrive in many phrasings (English or Roman Urdu "mein interested hu") and interleaved with other questions (ask "what is this job about" then "interested"), all of which must be interpreted correctly. Confidence: 0.9
- Works as a freelancer/contractor and insists on keeping client work strictly separate from their own personal projects — e.g., switching GitHub to a client account and using a per-repo identity so client pushes don't touch personal repos ("so i dont mess with my own projects"). Confidence: 0.7
- In client projects, is conscious of credential ownership boundaries: asks whether client-branded assets (e.g., the Telegram bot token for the client's notification bot) should be created/provided by the client rather than on the freelancer's own account, so the handoff stays clean — wants the agent to confirm who should own what before proceeding. Confidence: 0.5
- Needs infrastructure/deployment concepts explained in plain, non-technical terms — asked whether the project has to keep running on their laptop 24/7 ("what happens if i shutdown the laptop"), showing no prior familiarity with always-on VPS hosting; benefits from explicit "laptop is only for dev, server runs on its own" framing. Also proactively asks clarifying "what is this for / why is it needed?" questions about unfamiliar tools before using them (e.g., "is this for shopify or telegram and why is it needed?" about BotFather), so explain each component's role in the overall system up front. Reconfirmed when a widget didn't show up: asked "where should i expect the chatbot? after login or where" — needs to be told plainly where a feature will appear in the deployed product. Reconfirmed again when following the n8n workflow-import walkthrough: paused to ask "import from file, the import will be local file? because it asks me to upload from my computer.." — platform UI semantics (e.g., "Import from File" = file from the user's own computer, not the server) must be spelled out explicitly, and where the needed file lives on their machine should be pointed to. Reconfirmed during the Google OAuth setup: asked "look for advance wheere? in google console or in n8n?" — when a control/link is mentioned in instructions, must specify exactly which application and screen it appears on (Google Cloud Console vs the n8n dialog vs the accounts.google.com redirect page), since the user will not infer the right window from context alone. Confidence: 0.95
- Prefers the agent to handle mechanical config work itself — generating passwords/keys, filling placeholders, editing setup scripts — rather than being handed a list of things to edit ("Edit what? ADMIN_USER? Do it for me..."). The user only supplies values only they can obtain (bot tokens, chat IDs); everything else the agent should just do. Reconfirmed when they handed over a GitHub PAT with no other instruction ("token: ghp_...") and the agent was expected to wire it into git, build the boot script, and test the whole pipeline without further questions. Confidence: 0.9
- Shares credentials/tokens in plaintext directly in chat when required (pasted a GitHub PAT with just "token: ghp_..."), trusting the agent to use them and store them securely (e.g., chmod 600, root-only) rather than expecting any ceremony or back-and-forth about it. Confidence: 0.6
- Prefers to keep deployment momentum rather than block on missing inputs: when the user still has to obtain credentials (e.g., Telegram bot token / chat ID), proceed with all steps that don't need them (install Docker, start n8n, prep files) and defer only the credential-dependent parts — "I will give you those two as soon as I get it. But let's get it live and working yeah?" Reconfirmed when a BotFather link failed on their machine: they chose to set the token aside entirely ("let sit this one out for now") rather than debug the link error, and moved on to asking about overall deployment status. Reconfirmed when setting up the Google OAuth consent screen: asked "but if i set up external, i need to wait for a verification from google?" — wants the no-wait path (unverified external app + test user) so setup doesn't stall on a third-party review process. Reconfirmed again when the Telegram bot setup kept hitting friction: chose to cut scope and ship the core path only ("ok forget the bot notification for now, Let's just add it in google sheet only for now") — dropping the notification node entirely rather than debugging it, and deferring the optional feature to later. Reconfirmed when the n8n workflow kept erroring: proposed dropping the Telegram notification node altogether ("probably the telegram bot one is causing this issue. Just give me clean another n8n file") instead of debugging it. Confidence: 0.85
- Pastes raw terminal/error output verbatim into chat for diagnosis (e.g., the SSH bash heredoc error from sourcing the secrets file, the `git push` "fatal: No configured push destination" output, the full `git push -u origin main` rejection with fetch-first hints) and reports outcomes tersely ("opening local host says failed to load page", "1 and 2 done"), expecting the agent to diagnose and fix the root cause rather than explain the error back to them. Reconfirmed when a PowerShell command they ran mangled the quotes and errored on the VPS — they pasted the raw failure and the agent diagnosed the quoting issue and handed back a corrected command. Also pastes entire source files verbatim when diagnosing theme/deployment issues — pasted the complete `theme.liquid` in chat rather than describing what was in it, expecting the agent to spot the offending block. Confidence: 1.0
- Hands over files that live on their own machine by local path reference instead of pasting contents — when asked for the n8n workflow export, gave a Windows Downloads path in PowerShell syntax (`& 'c:\Users\nawas\Downloads\Recruitment — Candidate Intake (Telegram → Sheets → Admin Group).json' here you go`) expecting the agent to read it directly; the agent must explicitly ask for the file contents to be pasted (or have them re-export), since paths on the user's local machine are not accessible to the agent. Confidence: 0.5
- When a past integration attempt failed, prefers to reset to a clean baseline before retrying ("I had reverted back to the original theme, so let's start again") rather than patching the half-broken state — start the retry from a clean slate and redo the integration steps. Confidence: 0.7
- When resuming a project after a break/gap in sessions, wants the agent to re-orient them from scratch: recap where the project stands and lay out the concrete ordered next steps ("walk me through againn and whats my next steps") rather than assuming they remember prior context or making them dig through docs. Confidence: 0.6
- On servers that already host other work (their VPS has pre-existing deployments/directories), wants the agent to confirm and inspect the current state before proceeding, so nothing already deployed gets disturbed ("I have already few things deployed on that vps and directory etc. Can you confirm and continue with the steps"). Confidence: 0.6
- On client sites/storefronts that already have third-party widgets (e.g., a pre-existing Tidio chat on the Shopify store), refuses to touch or remove those existing integrations — "don't touch that chatbot it is not our place" — the scope is strictly to add/integrate the new widget without disturbing what's already there. Confidence: 0.9
- Edits Shopify theme files through the online editor where `theme-check` enforces strict Liquid/HTML validation (reported the `LiquidHTMLSyntaxError` "Attempting to end parsing before HtmlRawNode 'script' was closed"); expects embed code delivered to their theme to be validator-safe — a single clean external `<script src=...>` block rather than raw inline HTML/script that the editor can mangle or reject. Confidence: 0.6
- Treats client-site work as strictly additive and minimal: integrate only the chatbot they built and change NOTHING ELSE on the client's live website ("just integrate our chat and that's all. NOTHING ELSE should be change"). Pre-existing errors/issues on the client site are explicitly not their problem to fix ("before i applied any of the above, this error was still there so not our problem"). Confidence: 0.8
- Pastes handed-over code verbatim into platform editors without adapting it (pasted the HTML `<script src=...>` tag into Shopify's custom pixel editor, which only accepts JavaScript, and reported back the resulting "Expected an identifier and instead saw '<'" syntax error). Expects snippets supplied for a specific platform/editor to already conform to that editor's accepted syntax — e.g., a pure-JavaScript loader for the pixel editor, not an HTML tag. Confidence: 0.6
- Works in short iterative phases on the deployed product: confirms each milestone tersely ("integrated successfully and it works", "nice, the columns now show correctly. the changes are published", "ok good it works, but...") then immediately queues the next remaining issue ("but problem is still unused node use error. and no sheet update", "but it adds on random row, it adds on row 11 and only timestamp, source and status is added") — expects the agent to implement small fixes and redeploy without clarifying questions, then move on to the next tuning phase. Reconfirmed when, after the Normalize-node fix, he acknowledged "now it works perfectly" and instantly queued the next residual issue ("only thing remains is, it is entering values from 11 and so on") rather than pausing for sign-off. Reconfirmed again when, after the sheet landed correctly, he confirmed tersely ("works man") and immediately asked "so whats my next step" — expecting the agent to hand him the concrete ordered next action (exact commands to wire the chatbot to n8n and verify), not to recap or ask questions. Confidence: 0.9
- When resuming an interrupted session mid-task, wants the agent to just keep executing the already-approved plan to completion without re-presenting it or re-confirming scope — a single terse "ok continue, sorry for interution. CONTINUE" was enough for the agent to run a full multi-step implementation (PDF pipeline + guided flow + tests + deploy) end-to-end with no further check-ins. Reconfirmed with a bare "continue" (IDE open on an unrelated file) mid-debugging: the agent picked up the in-flight fix, finished removing the redundant guards, re-ran the suite and lint, and reported done without the user asking anything further. Reconfirmed again when the session hung mid-migration and the user sent "the system got hanged, continue if interupted" — the agent resumed the in-progress PDF-intents migration (flow.js, openai.js, generate-pdf.js, tests), fixed the remaining crashes and test expectations, and ran it to completion end-to-end without re-presenting the plan or re-confirming scope. Confidence: 0.75
- Reports back terse verification results and expects the agent to pivot the diagnosis when the checklist is exhausted: after confirming each UI-side item was already correct ("1- Sheet1, 2- headers, 3- Sheet1 set, still same error."), the agent is expected to stop re-checking the same surface-level causes, treat the persistent symptom as evidence of a deeper platform/config issue, and dig into server-side state (running container version, service logs) to find the root cause — not repeat the same instructions a fourth time. Reconfirmed when answering the agent's two diagnostic questions with numbered answers ("1- no, but after the webhook reply last node, im seeing -+ that's all. 2-it fails on first node, webhook, it is red.") — the terse ground-truth report (failure is at the webhook node itself) immediately redirected the diagnosis from the Respond-node theory to the webhook node. Reconfirmed when, after re-running the test, they reported the execution outcome at the trace level — "execution ran successfully but it ends up in reject invalid payload" — pinpointing the exact branch taken and redirecting the diagnosis straight to the IF-node's field paths. Reconfirmed when asked about the Sheets node's mapping: answered tersely with the exact node-config state — "values to send: {{ $json.timestamp }}{{ $json.name }}{{ $json.phone }} etc....." — handing over the ground-truth field contents for the agent to diagnose rather than explaining or speculating. Confidence: 0.85
- Corrects the agent's diagnosis when it contradicts what they can see firsthand in their own admin/platform (e.g., "there is only one active theme... no option for publish because it is already published"; cited the Shopify theme timeline showing "file saved and version history" as proof that saves work), and expects the agent to accept that ground truth and pivot the diagnosis rather than insist on the wrong theory. Reconfirmed when they overrode a "saves aren't landing" theory with concrete evidence — widget visible in preview mode on one store URL while the other URL showed only the pre-existing WhatsApp/Tidio widgets. Reconfirmed again when the agent kept referring to n8n's Document/Sheet fields as dropdowns: the user corrected that in their n8n version they are plain text/id input fields ("document and sheet both are the text fields, not dropdown, when i set document id, it takes me to the correct document, but sheet..."), and the agent accepted and pivoted to credential-first + direct id/name entry instructions. Confidence: 0.9
- Delegates live verification of the deployed product to the agent ("test the storefront") instead of running the checks himself — especially when his own inspection tools are blocked (view-source/console disabled on the client store); expects the agent to do its own live diagnostics rather than push every test back onto the user. Reconfirmed when, mid-debug, he made the Google Sheet public with a view link ("here is the view link,,i made the sheet public, check") so the agent could inspect the sheet's headers/rows itself instead of asking him to report them. Confidence: 0.7
- Proactively grants the agent direct read access to live data sources when a diagnosis depends on their state (made the Google Sheet public and shared the view link so the agent could verify the header row and partial rows directly) — prefers to hand over access for the agent to self-serve rather than re-describe or re-verify the data on the user's side. Confidence: 0.6
- When diagnosing a broken deployment, accepts the agent running its own server-side checks (fetching the deployed app's health endpoint over its public tunnel, grepping the project's `.env` on the agent's own machine) and reporting back the state, without pushing the testing back to the user. Reconfirmed during the n8n "No columns found" debug: after the user verified every UI-side checklist item (Sheet1 tab, header row, credential connected) and tersely reported "still same error", the agent SSH'd into the VPS, checked the running n8n container version, and grepped n8n's docker logs — finding the real root cause (`Forbidden - perhaps check your credentials?`, i.e., Google API permissions) rather than re-asking the user or repeating the same instructions. Confidence: 0.6
- Maintains multiple near-identically-named Shopify stores (production `job-portal-global.myshopify.com` vs a duplicate/sandbox `job-portal-global-2.myshopify.com`) and can unknowingly work in the wrong one for a long time; expects the agent to verify the actual target store from the admin URL (`admin.shopify.com/store/<handle>`) and theme ID before concluding a deployment is broken, since edits can land in the wrong storefront entirely; they may have admin visibility/access to only the duplicate store and not the production one at all (explicitly confirmed: "i can only see this store in stores. -2 one"), so once the wrong-store root cause is found, the remaining blocker is often account access to the production store, not code. Which store is "correct" is not static across sessions — in a later session the user re-confirmed `job-portal-global-2` as the live target and explained the old failure as "updating the correct theme but verifying on a different URL/store" — so always re-confirm the target store handle from the user/admin at the start of each session rather than assuming it from past context. Confidence: 0.9
- Wants the chatbot to be bilingual — English by default, but reply in Roman Urdu/Hinglish when the candidate writes that way (matching the user's own example pitch copy, e.g., "haan", "nahi", "aap", "kya") — with deterministic language detection rather than a model decision. Confidence: 0.7
- Expects the bot's knowledge to be produced as an actual shareable PDF deliverable of the live store's content ("You have the domain right, Make a PDF out of this... chatbot should be able to walk-through all of them"), with the same source content feeding the bot's answers so the PDF and the chat answers always stay in sync. Confidence: 0.7
- Copy-pastes terminal commands from chat into their SSH/terminal sessions verbatim, so commands get mangled by the paste and fail — `&&`-chained lines (`-bash: syntax error near unexpected token '&&'`), backslash-continuation curls (a literal newline landed inside the JSON string body, producing a 422 "Bad control character in string literal in JSON" from n8n), and even single-line curls with an inline `-d '{...}'` got duplicated/mangled (the command pasted twice, `-d` lost its parameter, and the JSON body ran as a shell command). Commands handed to the user to run themselves must be paste-proof: strictly single-line, and for anything carrying a payload prefer the file-based pattern — write the JSON to a temp file via heredoc (`cat > /tmp/... <<'EOF'`) then POST with `-d @/tmp/file.json` — so no inline payload survives to break, and have them run each command one at a time. Reconfirmed when even the heredoc/file pattern failed: the wrapped paste split `-d` from `@/tmp/test-lead.json` (curl: option -d: requires parameter) and injected line breaks inside the heredoc body, aborting it with `^C` — the only reliably paste-proof option turned out to be a single short command invoking a purpose-built test script already on the server (`sudo bash /opt/job-portal-chatbot/deploy/n8n-test-webhook.sh`) so no payload-shaped text is pasted at all. Reconfirmed yet again in a later exchange: even `-d @/tmp/test-lead.json` got split by the wrapped paste (curl: option -d: requires parameter; `@/tmp/test-lead.json` ran as a bare command), while the single short script command (`sudo bash /opt/job-portal-chatbot/deploy/n8n-test-webhook.sh`) succeeded cleanly. Reconfirmed in this exchange: when the agent went back to instructing the user to run `sudo bash /opt/job-portal-chatbot/deploy/n8n-test-webhook.sh`, the user still pasted a single-line `ssh -o ConnectTimeout=10 -o BatchMode=yes root@169.58.108.226 "docker ps ..."`-style diagnostic command back successfully — the short single-line command pattern remains the reliable one. Confidence: 0.99
- When an official platform/cloud UI (e.g., Google Cloud OAuth consent screen) shows a restriction or verification warning during setup (e.g., "OAuth access is restricted to the test users listed..."), asks for a quick verdict on whether it's expected and non-blocking for their own-use case ("is this okay?") plus the one corrective action needed (e.g., add the right account to Test users) — a short reassurance + actionable fix, not a lengthy explanation of the platform's policy. Confidence: 0.5
- Relays client-reported errors verbatim (quotes the exact message the client saw, e.g., "Sorry something went wrong, please try again") with minimal context and an urgent "We need to FIX this ASAP" — the client's symptom is the ground truth for the bug, and the agent is expected to trace the root cause through the codebase and ship the fix immediately without clarifying questions or lengthy explanations. Confidence: 0.6
- Tests the chatbot by role-playing an end user and reports the exact observed exchange verbatim — what they typed ("hi", "konsi jobs hain") and what the bot replied ("kept asking me are you interested") — tersely in Hinglish, treating the transcript as ground truth for the bug and expecting the agent to trace the state-machine/intent root cause and fix it without asking clarifying questions. Reconfirmed when reporting two further edge cases in a single message: the bot replying "mai sirf hamari jobs aur app mein madad kar sakta hun" for a job that IS in the store (loose-name mismatch), and the bot storing the user's own "konsi job ke liye apply kar rahe?" question as the candidate's name ("it stores as name... :facepalm:"). Confidence: 0.75
- Demands exhaustive, self-directed testing of the bot before sign-off: when failures are reported, expects the agent to trace root causes and then proactively hunt down and handle ALL possible failure cases ("JO JO failures ho sakte hain un sab ko handle karo... HAR TEST ko dekho aur handle karo") — universal escape hatches in every state, back-out phrases, every job name even under worst-case classifier output, misclassified knowledge/FAQ questions — not just patch the single reported bug. Explicitly asks for "MOST RUTHLESS TESTING" performed from the agent's side ("THOROUGH TESTING karo apni taraf sai"), so the agent should simulate adversarial user inputs and prove the fix across the whole matrix rather than push verification back to the user. Reconfirmed with an explicit deployment gate: the agent must itself build full conversation flows that try to BREAK the bot with valid use-cases, fine-tune iteratively, and deploy only after the extensive tests pass ("Khud se banao conversation flows, jisme chatbot ko BREAK karne ki puri koshish kro... Phir chatbot ko fine-tune karo, and after passing extensive tests, then we will deploy the final version"). Restated again when the user closed a dense chatbot spec with "HANDLE AND TEST ALL SCENARIOS." Confidence: 0.95
ro, and after passing extensive tests, then we will deploy the final version"). Confidence: 0.9
- When confirming that obsolete code is gone, wants the claim verified by an actual repo-wide sweep (grep for old identifiers, list knowledge/ dir, read README/package.json/.env.example) rather than a hand-wave "yes" — and expects the agent to distinguish what the runtime actually uses vs. dead residue (orphaned data files, broken scraper scripts, stale docs/env-templates describing the old pipeline) and to proactively list the exact files that contradict the new "PDF-only" story before asking for destructive-delete approval. Reconfirmed when the user pushed for a "100% confident?" verdict on the PDF-only knowledgebase before deploying — the answer had to come from a repo-wide sweep (grep for scraped content, read of loader/base files, git tracking check) not an assertion. Confidence: 0.8
- The bar for cleanup/migration is 100% complete, explicitly not "most": after the agent flagged leftover artifacts, the user approved with "perfect, remove/delete any stale residue, Only the pdf knowloedgebase should be working and IT SHOULD MUST BE 100% not most" — authorizing destructive deletion of all orphaned files (old scrape markdown/PDF, dead scraper script) AND expecting the agent to make the surviving pipeline fully reproducible, not just remove files: declare the previously-undeclared pdf-parse devDependency, fix the extractor's wrong package API, replace the throwaway temp script with a permanent one, wire npm scripts (extract/build:knowledge), and update package.json/README/.env.example so zero stale references remain. Any leftover dead file, stale doc, or silently-broken tooling counts as "most" and is rejected. Confidence: 0.85
